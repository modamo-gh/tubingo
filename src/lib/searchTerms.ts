export const searchTerms = {
	old: [
		{
			keyPhrase: "IMG",
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: "MVI",
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `WIN ${new Date().getFullYear()}${String(
				new Date().getMonth() + 1
			).padStart(2, "0")}${String(new Date().getDate()).padStart(
				2,
				"0"
			)}`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `${new Date().getFullYear()}${String(
				new Date().getMonth() + 1
			).padStart(2, "0")}${String(new Date().getDate()).padStart(
				2,
				"0"
			)}`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `VID ${new Date().getFullYear()}${String(
				new Date().getMonth() + 1
			).padStart(2, "0")}${String(new Date().getDate()).padStart(
				2,
				"0"
			)}`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `${String(new Date().getMonth() + 1).padStart(
				2,
				"0"
			)}${String(new Date().getDate()).padStart(
				2,
				"0"
			)}${new Date().getFullYear()}`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `Capture ${new Date().getFullYear()}${String(
				new Date().getMonth() + 1
			).padStart(2, "0")}${String(new Date().getDate()).padStart(
				2,
				"0"
			)}`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `${String(new Date().getDate()).padStart(
				2,
				"0"
			)}${String(new Date().getMonth() + 1).padStart(
				2,
				"0"
			)}${new Date().getFullYear()}`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: "My Movie",
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: "My Edited Video",
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: "/Storage/Emulated/",
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: "PXL",
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `InShot ${new Date().getFullYear()}${String(
				new Date().getMonth() + 1
			).padStart(2, "0")}${String(new Date().getDate()).padStart(
				2,
				"0"
			)}`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `WhatsApp Video ${new Date().getFullYear()}`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `FullSizeRender`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `RPReplay`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `VTS 01`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `DVR`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `SWF`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `VLC Record`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `Robloxapp`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.MP4`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.3GP`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.MOV`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.AVI`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.WMV`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.MKV`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.MPEG`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.FLV`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.FLAC`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: `.WAV`,
			searchParameter: "CAISBAgCEAE%253D",
			toString: function () {
				return `${this.keyPhrase} ${this.searchParameter}`;
			}
		}
	],
	new: [
		{
			keyPhrase: () =>
				`IMG ${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`MVI ${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`MOV ${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`100 ${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`SAM ${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`DSC ${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`SDV ${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`DSCF${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`DSCN${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`PICT${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`MAQ0${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`FILE${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`GOPR${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`GP01${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`GX01${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`DJI ${String(Math.floor(Math.random() * 10000)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`HNI 0${String(Math.floor(Math.random() * 101)).padStart(
					3,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`WA0${String(Math.floor(Math.random() * 1000)).padStart(
					3,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`MOL0${Math.floor(Math.random() * 256)
					.toString(16)
					.toUpperCase()
					.padStart(2, "0")}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`MOL0${String(Math.floor(Math.random() * 101)).padStart(
					4,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`${String(Math.floor(Math.random() * 24)).padStart(
					2,
					"0"
				)}${String(Math.floor(Math.random() * 60)).padStart(
					2,
					"0"
				)}${String(Math.floor(Math.random() * 60)).padStart(2, "0")}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`P10${
					Math.random() < 0.8
						? String(Math.floor(Math.random() * 20000)).padStart(
								5,
								"0"
						  )
						: String(
								Math.floor(Math.random() * 80000) + 20000
						  ).padStart(5, "0")
				}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`VTS ${String(Math.floor(Math.random() * 100)).padStart(
					2,
					"0"
				)} ${String(Math.floor(Math.random() * 10))}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`VTS ${String(Math.floor(Math.random() * 1000)).padStart(
					3,
					"0"
				)} 1`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`VTS 01 ${String(Math.floor(Math.random() * 1000)).padStart(
					3,
					"0"
				)}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => "My Slideshow",
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`My Slideshow ${String(
					Math.floor(Math.random() * 100)
				).padStart(2, "0")}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => "My Slideshow Video",
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => "My Stupeflix Video",
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () =>
				`My Stupeflix Video ${String(
					Math.floor(Math.random() * 1051)
				).padStart(4, "0")}`,
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => {
				const startYear = 2006;
				const start = new Date(startYear, 0, 1);
				const end = new Date();

				const randTime =
					start.getTime() +
					Math.random() * (end.getTime() - start.getTime());
				const randDate = new Date(randTime);

				return `${randDate.getFullYear()}${String(
					randDate.getMonth() + 1
				).padStart(2, "0")}${String(randDate.getDate()).padStart(
					2,
					"0"
				)}`;
			},
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => {
				const startYear = 2006;
				const start = new Date(startYear, 0, 1);
				const end = new Date();

				const randTime =
					start.getTime() +
					Math.random() * (end.getTime() - start.getTime());
				const randDate = new Date(randTime);

				return `${String(randDate.getMonth() + 1).padStart(
					2,
					"0"
				)}${String(randDate.getDate()).padStart(
					2,
					"0"
				)}${randDate.getFullYear()}`;
			},
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => {
				const startYear = 2006;
				const start = new Date(startYear, 0, 1);
				const end = new Date();

				const randTime =
					start.getTime() +
					Math.random() * (end.getTime() - start.getTime());
				const randDate = new Date(randTime);

				return `${String(randDate.getDate()).padStart(2, "0")}${String(
					randDate.getMonth() + 1
				).padStart(2, "0")}${randDate.getFullYear()}`;
			},
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => {
				const startYear = 2006;
				const start = new Date(startYear, 0, 1);
				const end = new Date();

				const randTime =
					start.getTime() +
					Math.random() * (end.getTime() - start.getTime());
				const randDate = new Date(randTime);

				return `WIN ${randDate.getFullYear()}${String(
					randDate.getMonth() + 1
				).padStart(2, "0")}${String(randDate.getDate()).padStart(
					2,
					"0"
				)}`;
			},
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => {
				const startYear = 2006;
				const start = new Date(startYear, 0, 1);
				const end = new Date();

				const randTime =
					start.getTime() +
					Math.random() * (end.getTime() - start.getTime());
				const randDate = new Date(randTime);

				return `VID ${randDate.getFullYear()}${String(
					randDate.getMonth() + 1
				).padStart(2, "0")}${String(randDate.getDate()).padStart(
					2,
					"0"
				)}`;
			},
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => {
				const startYear = 2006;
				const start = new Date(startYear, 0, 1);
				const end = new Date();

				const randTime =
					start.getTime() +
					Math.random() * (end.getTime() - start.getTime());
				const randDate = new Date(randTime);

				return `Capture ${randDate.getFullYear()}${String(
					randDate.getMonth() + 1
				).padStart(2, "0")}${String(randDate.getDate()).padStart(
					2,
					"0"
				)}`;
			},
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => {
				const startYear = 2017;
				const start = new Date(startYear, 0, 1);
				const end = new Date();

				const randTime =
					start.getTime() +
					Math.random() * (end.getTime() - start.getTime());
				const randDate = new Date(randTime);

				return `InShot ${randDate.getFullYear()}${String(
					randDate.getMonth() + 1
				).padStart(2, "0")}${String(randDate.getDate()).padStart(
					2,
					"0"
				)}`;
			},
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => {
				const startYear = 2018;
				const start = new Date(startYear, 0, 1);
				const end = new Date();

				const randTime =
					start.getTime() +
					Math.random() * (end.getTime() - start.getTime());
				const randDate = new Date(randTime);

				return `AUD-${randDate.getFullYear()}${String(
					randDate.getMonth() + 1
				).padStart(2, "0")}${String(randDate.getDate()).padStart(
					2,
					"0"
				)}`;
			},
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		},
		{
			keyPhrase: () => {
				const startYear = 2021;
				const start = new Date(startYear, 0, 1);
				const end = new Date();

				const randTime =
					start.getTime() +
					Math.random() * (end.getTime() - start.getTime());
				const randDate = new Date(randTime);

				return `PXL ${randDate.getFullYear()}${String(
					randDate.getMonth() + 1
				).padStart(2, "0")}${String(randDate.getDate()).padStart(
					2,
					"0"
				)}`;
			},
			searchParameter: "",
			toString: function () {
				return `${this.keyPhrase()} ${this.searchParameter}`;
			}
		}
	]
};
